get: SELECT role.* FROM role WHERE role.id = :id;
getAll: SELECT role.* FROM role;

upsert {
    UPDATE role
    SET colour = :colour,
        name = :name
    WHERE id = :id;

    INSERT OR IGNORE INTO role(id, colour, name)
        VALUES (:id, :colour, :name);
}

drop {
    DELETE FROM user_role WHERE role_id = :id;
    DELETE FROM role WHERE id = :id;
}
